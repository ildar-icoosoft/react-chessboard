import React, { FC } from "react";
import { PieceColor } from "../enums/PieceColor";

export interface CoordsProps {
  orientation: PieceColor;
}

export const Coords: FC<CoordsProps> = ({}) => {
  return <div></div>;
};
